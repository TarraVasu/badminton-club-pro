import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.birdiebeasts.app',
  appName: 'Birdie Beasts',
  webDir: 'dist/badminton-app'
};

export default config;
